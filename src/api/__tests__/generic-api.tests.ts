// @ts-ignore

import {genericController} from "../generic/generic-api";

describe('Tests Generic API',()=>{
    it('Carrega um schema para um resource',async () =>{
        //const schema = await genericController<People>("people").getSchema();
        //expect(schema).toBeTruthy();
    })

    it('Busca por um id existente',async ()=>{
        //const people = await peopleController.getById(1);
        //expect(people).toBeTruthy();
    })
})
